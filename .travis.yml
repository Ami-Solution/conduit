dist: xenial
language: python

addons:
  apt_packages:
    - nodejs
    - npm
    - redis-server
    
python:
    - 3.7
sudo: required
services:
    - docker
script: 
    - pip install node
    - yarn add dotenv
    - pip install docker-compose
#    - npm install -g npx
    - yarn start
#    - npx @0x/launch-kit-wizard && 
#    - docker-compose up
#    - docker-machine -v
